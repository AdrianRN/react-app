
import CancelIcon from '@mui/icons-material/Cancel';
import { styled } from '@mui/system';

const CancelErrorIcon = styled(CancelIcon)({
  fontSize: '20px', // Cambia el tamaño del icono
  color: '#E40173',   // Cambia el color del icono
});

export default CancelErrorIcon;   
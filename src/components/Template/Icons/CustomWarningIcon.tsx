
import ErrorIcon from '@mui/icons-material/Error';
import { styled } from '@mui/system';

const CustomWarningIcon = styled(ErrorIcon)({
  fontSize: '20px', // Cambia el tamaño del icono
  color: '#DCC80F',   // Cambia el color del icono
});

export default CustomWarningIcon;   
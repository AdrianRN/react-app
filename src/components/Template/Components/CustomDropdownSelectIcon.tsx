import React from 'react';
import CustomDropdownIcon from '../../OuiComponents/Icons/Frame';

const CustomDropdownSelectIcon = (props:any) => (
  <CustomDropdownIcon {...props} /> // Ajusta esto según cómo estés renderizando tu icono personalizado
);

export default CustomDropdownSelectIcon;